## Input:DeadState.kt

```kt
package example.dead

import koma.core.Action
import koma.core.State
import me.tbsten.koma.strict.OnAction
import me.tbsten.koma.strict.StoreSpec

@StoreSpec
sealed interface DeadState : State {
    companion object

    @OnAction<DeadAction.Used>(nextState = [Done::class])
    interface Idle : DeadState { companion object }

    interface Done : DeadState { companion object }
}

sealed interface DeadAction : Action {
    data object Used : DeadAction
    data object Unused : DeadAction   // どの state もハンドルしない
}
```

## KSP options

```kt
ksp {
    arg("koma.strict.deadActionSeverity", "WARNING" /* default */)
}
```

## Output:ExitCode

```text
OK
```

## Output:Console

```text
w: [ksp] <TMPDIR>/Kotlin-Compilation<N>/sources/DeadState.kt:20: koma-strict: action 'example.dead.DeadAction.Unused' is never handled by any state (dead action).
```

## Output:Generated sources

```kt
// file: DeadState.Done.generated.kt
package example.dead

import me.tbsten.koma.strict.*
import me.tbsten.koma.strict.dsl.*

private data object DoneImpl : DeadState.Done

public operator fun DeadState.Done.Companion.invoke(): DeadState.Done = DoneImpl

// ----- next file -----

// file: DeadState.Idle.generated.kt
package example.dead

import me.tbsten.koma.strict.*
import me.tbsten.koma.strict.dsl.*

private data object IdleImpl : DeadState.Idle

public operator fun DeadState.Idle.Companion.invoke(): DeadState.Idle = IdleImpl

public sealed interface IdleUsedReaction {
    public class Transition internal constructor(
        internal val next: DeadState,
    ) : IdleUsedReaction
}

public class IdleUsedTransitions internal constructor(
    @Suppress("unused") private val state: DeadState.Idle,
) {
    public fun toDone(): IdleUsedReaction = IdleUsedReaction.Transition(DeadState.Done())
}

@KomaStrictDsl
@koma.core.KomaStoreDsl
@OptIn(InternalKomaStrictApi::class)
public class IdleUsedScope internal constructor(
    public val state: DeadState.Idle,
    public val action: DeadAction.Used,
    onClearPendingActions: () -> Unit,
) : HandlerScope(onClearPendingActions) {
    public val nextState: IdleUsedTransitions = IdleUsedTransitions(state)
}

public class IdleHandlers internal constructor(
    internal val used: suspend IdleUsedScope.() -> IdleUsedReaction,
    internal val configure: koma.core.StoreBuilder.StateHandlerConfig<DeadState, DeadAction, Nothing, DeadState.Idle>.() -> Unit,
) : (IdleHandlersScope) -> IdleHandlers {
    /** Returns this bundle itself, so an already-built value is assignable to the scope-lambda-typed `states(...)` parameter (value-passing / scope-lambda interchange). */
    override fun invoke(p1: IdleHandlersScope): IdleHandlers = this
}

/** Bundles this state's handlers. [configure] appends raw koma DSL at the end of the generated `state<...> {}` block (per-state escape hatch). */
public fun DeadState.Idle.Companion.actions(
    used: suspend IdleUsedScope.() -> IdleUsedReaction,
    configure: koma.core.StoreBuilder.StateHandlerConfig<DeadState, DeadAction, Nothing, DeadState.Idle>.() -> Unit = {},
): IdleHandlers = IdleHandlers(used, configure)

/**
 * Builder-form overload of `actions(...)` (see [IdleActionsBuilder]).
 *
 * Note: unlike the named-argument overload, exhaustiveness of the declared handlers is
 * checked **at build time** (fail-fast when the block finishes), not at compile time.
 * This overload exists only for states without enter / exit declarations (those handlers
 * have no builder member).
 */
public fun DeadState.Idle.Companion.actions(build: IdleActionsBuilder.() -> Unit): IdleHandlers = IdleActionsBuilder().apply(build).build()

/** Receiver of the scope-lambda form (`{ actions(...) }`) of the matching `states(...)` parameter. [actions] mirrors the companion `actions(...)` extension. */
@KomaStrictDsl
@koma.core.KomaStoreDsl
public class IdleHandlersScope internal constructor() {
    /** Bundles this state's handlers. [configure] appends raw koma DSL at the end of the generated `state<...> {}` block (per-state escape hatch). */
    public fun actions(
        used: suspend IdleUsedScope.() -> IdleUsedReaction,
        configure: koma.core.StoreBuilder.StateHandlerConfig<DeadState, DeadAction, Nothing, DeadState.Idle>.() -> Unit = {},
    ): IdleHandlers = IdleHandlers(used, configure)

    /**
     * Builder-form overload of `actions(...)` (see [IdleActionsBuilder]).
     *
     * Note: unlike the named-argument overload, exhaustiveness of the declared handlers is
     * checked **at build time** (fail-fast when the block finishes), not at compile time.
     * This overload exists only for states without enter / exit declarations (those handlers
     * have no builder member).
     */
    public fun actions(build: IdleActionsBuilder.() -> Unit): IdleHandlers = IdleActionsBuilder().apply(build).build()
}

/**
 * Builder receiver of the `actions { ... }` overload building [IdleHandlers].
 *
 * Each member function registers the handler of the same-named `actions(...)` parameter.
 * Exhaustiveness is checked when the block finishes (build-time fail-fast), not at compile
 * time: handlers left unregistered — and any duplicate registration — throw
 * [IllegalStateException]. Prefer the named-argument `actions(...)` overload to catch
 * missing handlers at compile time.
 */
@KomaStrictDsl
@koma.core.KomaStoreDsl
@OptIn(InternalKomaStrictApi::class)
public class IdleActionsBuilder internal constructor() {
    private val used = SetOnceSlot<suspend IdleUsedScope.() -> IdleUsedReaction>("DeadState.Idle", "used")

    private val configure = SetOnceSlot<koma.core.StoreBuilder.StateHandlerConfig<DeadState, DeadAction, Nothing, DeadState.Idle>.() -> Unit>("DeadState.Idle", "configure")

    /** Registers the handler of the same-named `actions(...)` parameter. Duplicate registration fails fast with [IllegalStateException]. */
    public fun used(handler: suspend IdleUsedScope.() -> IdleUsedReaction) { used.set(handler) }

    /** Appends raw koma DSL at the end of the generated `state<...> {}` block (the trailing `configure` parameter of `actions(...)`). Duplicate registration fails fast with [IllegalStateException]. */
    public fun configure(block: koma.core.StoreBuilder.StateHandlerConfig<DeadState, DeadAction, Nothing, DeadState.Idle>.() -> Unit) { configure.set(block) }

    internal fun build(): IdleHandlers {
        val missing = listOfNotNull(
            "used".takeIf { !used.isSet },
        )
        if (missing.isNotEmpty()) throwMissingBuilderEntries("DeadState.Idle", missing)
        return IdleHandlers(used.getOrNull()!!, configure.getOrNull() ?: {})
    }
}

// ----- next file -----

// file: DeadState.storeSpec.generated.kt
package example.dead

import me.tbsten.koma.strict.*
import me.tbsten.koma.strict.dsl.*

@kotlin.jvm.JvmName("deadStateStates")
@Suppress("NAME_SHADOWING")
public fun koma.core.StoreBuilder<DeadState, DeadAction, Nothing>.states(
    idle: IdleHandlersScope.() -> IdleHandlers,
    configure: DeadStateStatesConfigureScope.() -> Unit = {},
) {
    val idle = IdleHandlersScope().idle()
    val configure = DeadStateStatesConfigureScope().apply(configure)

    state<DeadState.Idle> {
        action<DeadAction.Used> {
            when (val r = idle.used(IdleUsedScope(state, action, ::clearPendingActions))) {
                is IdleUsedReaction.Transition -> nextState { r.next }
            }
        }
        idle.configure(this)
        configure.idle?.invoke(this)
    }
}

/**
 * Builds a [koma.core.Store] for [DeadState] without spelling the store type arguments.
 *
 * Sugar over the canonical koma entry point — `Store<DeadState, DeadAction, Nothing>(initialState) { states(...) }`
 * builds the exact same store. [configuration] appends raw koma DSL after the generated
 * handlers (store-level escape hatch).
 */
public fun restoreDeadStore(
    initialState: DeadState,
    idle: IdleHandlersScope.() -> IdleHandlers,
    context: kotlin.coroutines.CoroutineContext? = null,
    configuration: koma.core.StoreBuilder<DeadState, DeadAction, Nothing>.() -> Unit = {},
): koma.core.Store<DeadState, DeadAction, Nothing> =
    koma.core.Store<DeadState, DeadAction, Nothing>(initialState = initialState, context = context) {
        states(
            idle = idle,
        )
        configuration()
    }

/**
 * Receiver of the trailing `configure` block of the matching `states(...)` call — the
 * per-state escape hatch. Each member appends raw koma DSL to the generated `state<...> {}`
 * block(s) of the same-named child state, after the generated registrations (and after the
 * leaf's own `actions(configure = ...)` block). Calling the same member twice fails fast
 * with [IllegalStateException].
 *
 * Overlapping registrations (measured against koma-core rc02): koma dispatches at most one
 * handler per trigger — when multiple registered handlers match the same state and trigger,
 * the **first registered** one runs and later ones are silently ignored. Generated
 * registrations always precede this escape block, so a raw handler for a trigger already
 * declared on the state never runs; use this escape for what the declarations do not cover
 * (raw `enter` / `exit` / `action<A>` / `recover<T>` of undeclared triggers, `launch`,
 * dispatcher overrides, ...).
 */
@KomaStrictDsl
@koma.core.KomaStoreDsl
public class DeadStateStatesConfigureScope internal constructor() {
    internal var idle: (koma.core.StoreBuilder.StateHandlerConfig<DeadState, DeadAction, Nothing, DeadState.Idle>.() -> Unit)? = null

    /** Appends raw koma DSL at the end of the generated `state<DeadState.Idle> {}` block. Fails fast if called twice. */
    public fun idle(block: koma.core.StoreBuilder.StateHandlerConfig<DeadState, DeadAction, Nothing, DeadState.Idle>.() -> Unit) {
        if (this.idle != null) throwDuplicateBuilderEntry("DeadState", "idle")
        this.idle = block
    }
}
```
